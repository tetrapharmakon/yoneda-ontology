#!/bin/env bash

case "$1" in
"I")
latexmk -pdf -shell-escape -pvc -jobname=I-identity/I-identity I-identity/I-identity.tex
latexmk -c ;;
"II")
latexmk -pdf -shell-escape -pvc -jobname=I-identity/I-identity.pdf II-spheres.tex
latexmk -c ;;
"III")
latexmk -pdf -shell-escape -pvc -jobname=I-identity/I-identity.pdf III-homot.tex
latexmk -c ;;
"IV")
latexmk -pdf -shell-escape -pvc -jobname=I-identity/I-identity.pdf IV-mereo.tex
latexmk -c ;;
"all")
latexmk -pdf -shell-escape I-identity
latexmk -pdf -shell-escape II-spheres
latexmk -pdf -shell-escape III-homot
latexmk -pdf -shell-escape IV-mereo
latexmk -c ;;
"d")
latexmk -pdf notes_denta.tex ;;
"app")
cd appmeta/
latexmk -pdf appmeta.tex 
cd ..;;
"git")
git add -A 
git commit -m "automated update"
git push ;;
esac
